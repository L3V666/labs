import numpy as np

R = np.array([16500, 19300, 22000, 24800, 27500, 30300, 33000, 38500, 44000, 49500, 55000])

Teta = np.array([2.23, 1.88, 1.67, 1.51, 1.37, 1.27, 1.18, 1.03, 0.86, 0.78, 0.78])

R_0 = 610

R_cr = (R + R_0) / (np.sqrt((2 * np.pi / Teta) ** 2 + 1)) - R_0

print(R_cr)